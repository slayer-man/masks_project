# pytest --cache-clear --cov=src --cov-report term-missing

# pytest --cov=src --cov-report=html

# pip freeze > requirements.txt

# pip install -r requirements.txt

# .venv\Scripts\activate

# deactivate

# git commit -am 'modified'

# git commit -am 'add search.py, test_search.py & modified exchendge_rate.py, external_api.py, generators.py, utils.py, widget.py & test_(exchendge_rate.py, external_api.py, generators.py, utils.py, widget.py)'

# Менеджер банковских транзакций (Masks Project)

Профессиональное консольное приложение на Python для фильтрации, сортировки, маскирования и анализа банковских операций. Программа поддерживает работу с тремя популярными форматами данных (**JSON, CSV, XLSX**) и интегрирована с внешним API для автоматической конвертации валютных транзакций в рубли.

## 🛠️ Технологический стек

* **Язык**: Python 3.14+
* **Анализ данных**: Pandas, OpenPyXL
* **Сеть**: Requests, Python-dotenv
* **Тестирование**: Pytest, Pytest-cov, Unittest.mock
* **Качество кода**: Black, Flake8, Mypy

---

## 📂 Архитектура проекта

```text
Masks_Project/
│
├── data/                         # Входные файлы данных
│   ├── operations.json           # Выгрузка транзакций в формате JSON
│   ├── transactions.csv          # Выгрузка транзакций в формате CSV
│   └── transactions_excel.xlsx   # Выгрузка транзакций в формате Excel
│
├── logs/                         # Автоматически создаваемые логи работы модулей
│   ├── masks.log                 # Логирование маскирования карт и счетов
│   └── exchange_rate.log         # Логирование сетевых запросов и работы кэша
│
├── src/                          # Пакет с исходным кодом модулей
│   ├── __init__.py
│   ├── decorators.py             # Декораторы логирования функций
│   ├── exchange_rate.py          # Логика кэширования валют и чтения JSON
│   ├── external_api.py           # Сетевое взаимодействие с внешним API
│   ├── generators.py             # Генераторы для эффективной обработки массивов
│   ├── masks.py                  # Маскирование номеров карт и счетов
│   ├── processing.py             # Фильтрация по статусам и сортировка дат
│   ├── search.py                 # Поиск по регулярным выражениям (re) и Counter
│   ├── tables.py                 # Модуль табличной обработки данных
│   ├── utils.py                  # Парсеры плоских таблиц CSV и XLSX
│   └── widget.py                 # Обработка пользовательских форматов и дат
│
├── tests/                        # Набор модульных и интеграционных тестов
│
├── .env                          # Конфигурация переменных окружения (API Ключи)
├── .flake8                       # Настройки совместимости линтера Flake8 и Black
├── pyproject.toml                # Конфигурация проекта и утилиты Black
└── main.py                       # Главный модуль запуска интерактивного меню
```

---

## 🚀 Установка и настройка

### 1. Клонирование и переход в проект
```bash
cd D:\Python\Projects\Masks_Project
```

### 2. Активация виртуального окружения
```bash
.venv\Scripts\activate
```

### 3. Настройка переменных окружения
Создайте файл **`.env`** в корневой папке проекта и добавьте ваш приватный токен для работы с актуальными курсами валют:
```env
API_KEY=ваш_секретный_токен_с_apilayer
```

---

## 🕹️ Пользовательский интерфейс (Интерактивное меню)

Приложение предоставляет удобное текстовое меню, защищенное от опечаток и некорректного ввода:

1. **Выбор источника данных**: Пользователь выбирает формат (`1` — JSON, `2` — CSV, `3` — XLSX).
2. **Фильтрация по статусу**: Выбор интересующих операций с помощью цифровых клавиш (`1` — EXECUTED, `2` — CANCELED, `3` — PENDING).
3. **Параметры сортировки**: Опциональное включение сортировки дат по возрастанию или убыванию (`1` или `2`).
4. **Валютная фильтрация**: Возможность отобразить только рублевые операции либо автоматически сконвертировать иностранную валюту (USD/EUR) по актуальному курсу.
5. **Поиск по регулярным выражениям**: Фильтрация итоговой таблицы по любому ключевому слову в описании (поле `description`) с использованием библиотеки `re`.

---

## 🧪 Тестирование и качество кода

Проект разработан по методологии безопасной разработки и полностью покрыт автоматическими тестами. Сетевые запросы и дисковые операции изолированы с помощью фикстур `unittest.mock`.

### Запуск тестов и сбор метрик покрытия (Coverage):
```bash
pytest --cache-clear --cov=src --cov-report term-missing
```
*Текущий показатель покрытия проекта тестами составляет **98%**.*

### Статический анализ типов (Mypy):
```bash
mypy src/search.py
```

### Проверка стиля кода и форматирование:
```bash
black src/
flake8 src/
```

---

## 🛡️ Отказоустойчивость и Логирование

* **Отказоустойчивость сети (Fallback)**: В случае падения интернета, блокировки запросов или исчерпания лимитов API (`Read timed out`), приложение автоматически переключается на чтение локального файла-кэша `data/exchange_rates.json`. Работа пользователя не прерывается.
* **Логирование**: Все критические события, статусы HTTP-ответов сервера (например, 200, 401, 429) и отладочная информация пишутся в фоновом режиме в файлы папки `logs/` в кодировке `UTF-8`.
